set name = man1

all.role manager
all.manager localhost:20990

xrd.port 10990 if exec xrootd
xrd.port 20990 if exec cmsd

if exec xrootd
xrd.protocol XrdHttp:10990 libXrdHttp.so
fi

continue @CMAKE_CURRENT_BINARY_DIR@/common.cfg

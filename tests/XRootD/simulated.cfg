set name = simulated
set port = 6543

set src = $SOURCE_DIR
set pwd = $PWD

xrootd.trace all
pfc.trace info

xrootd.seclib libXrdSec.so

ofs.osslib libXrdOssSimulated.so
ofs.xattrlib libXrdOssSimulated.so

continue $src/common.cfg

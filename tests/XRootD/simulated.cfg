set name = simulated
set port = 6543

set src = $SOURCE_DIR
set pwd = $PWD

xrootd.trace all
pfc.trace info

xrootd.seclib libXrdSec.so

ofs.osslib libXrdSimulated.so
ofs.xattrlib libXrdSimulatedXAttr.so

continue $src/common.cfg

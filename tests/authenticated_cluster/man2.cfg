set name = man2

all.role manager
all.manager meta localhost:20970
all.manager localhost:20972

xrd.port 10972 if exec xrootd
xrd.port 20972 if exec cmsd

if exec xrootd
xrd.protocol XrdHttp:10972 libXrdHttp.so
fi

continue @CMAKE_CURRENT_BINARY_DIR@/common.cfg

set name = metaman

all.role meta manager
all.manager meta localhost:20970

xrd.port 10970 if exec xrootd
xrd.port 20970 if exec cmsd

if exec xrootd
xrd.protocol XrdHttp:10970 libXrdHttp.so
fi

continue @CMAKE_CURRENT_BINARY_DIR@/common.cfg

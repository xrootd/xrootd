set name = man1

all.role manager
all.manager meta localhost:20970
all.manager localhost:20971

xrd.port 10971 if exec xrootd
xrd.port 20971 if exec cmsd

if exec xrootd
xrd.protocol XrdHttp:10971 libXrdHttp.so
fi

continue @CMAKE_CURRENT_BINARY_DIR@/common.cfg

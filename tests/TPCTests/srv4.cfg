set name = srv4

all.role server

xrd.port 10954

if exec xrootd
xrd.protocol XrdHttp:10954 libXrdHttp.so
fi

# setup.sh makes data/srv4 a link to data/srv1: a filesystem shared with srv1
# that xrootd cannot see.
tpc.dfs localhost

continue @CMAKE_CURRENT_BINARY_DIR@/common.cfg
